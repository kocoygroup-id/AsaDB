# Third-Party Notices

AsaDB v1.0.0 includes a portable runtime built with SWI-Prolog and related runtime libraries. Those files are redistributed as runtime dependencies so `AsA.exe` can run on Windows without requiring users to install SWI-Prolog separately.

Third-party components are not owned by AsaDB and remain under their respective upstream licenses.

Recommended upload note: keep this file with the package when distributing the runtime.
