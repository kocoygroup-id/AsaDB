/*  Creator: make/0

    Purpose: Provide index for autoload
*/

index(crypto_n_random_bytes(?,?), crypto, crypto).
index(crypto_data_hash(?,?,?), crypto, crypto).
index(crypto_file_hash(?,?,?), crypto, crypto).
index(crypto_context_new(?,?), crypto, crypto).
index(crypto_data_context(?,?,?), crypto, crypto).
index(crypto_context_hash(?,?), crypto, crypto).
index(crypto_open_hash_stream(?,?,?), crypto, crypto).
index(crypto_stream_hash(?,?), crypto, crypto).
index(crypto_password_hash(?,?), crypto, crypto).
index(crypto_password_hash(?,?,?), crypto, crypto).
index(crypto_data_hkdf(?,?,?,?), crypto, crypto).
index(ecdsa_sign(?,?,?,?), crypto, crypto).
index(ecdsa_verify(?,?,?,?), crypto, crypto).
index(crypto_data_decrypt(?,?,?,?,?,?), crypto, crypto).
index(crypto_data_encrypt(?,?,?,?,?,?), crypto, crypto).
index(hex_bytes(?,?), crypto, crypto).
index(rsa_private_decrypt(?,?,?,?), crypto, crypto).
index(rsa_private_encrypt(?,?,?,?), crypto, crypto).
index(rsa_public_decrypt(?,?,?,?), crypto, crypto).
index(rsa_public_encrypt(?,?,?,?), crypto, crypto).
index(rsa_sign(?,?,?,?), crypto, crypto).
index(rsa_verify(?,?,?,?), crypto, crypto).
index(crypto_modular_inverse(?,?,?), crypto, crypto).
index(crypto_generate_prime(?,?,?), crypto, crypto).
index(crypto_is_prime(?,?), crypto, crypto).
index(crypto_name_curve(?,?), crypto, crypto).
index(crypto_curve_order(?,?), crypto, crypto).
index(crypto_curve_generator(?,?), crypto, crypto).
index(crypto_curve_scalar_mult(?,?,?,?), crypto, crypto).
index(saml_authenticate(?,?,?,?), saml, saml).
index(certificate_field(?,?), ssl, ssl).
index(load_certificate(?,?), ssl, ssl).
index(load_private_key(?,?,?), ssl, ssl).
index(load_public_key(?,?), ssl, ssl).
index(load_crl(?,?), ssl, ssl).
index(write_certificate(?,?,?), ssl, ssl).
index(system_root_certificates(?), ssl, ssl).
index(cert_accept_any(?,?,?,?,?), ssl, ssl).
index(same_certificate(?,?), ssl, ssl).
index(ssl_context(+,-,:), ssl, ssl).
index(ssl_add_certificate_key(?,?,?,?), ssl, ssl).
index(ssl_set_options(+,-,:), ssl, ssl).
index(ssl_property(?,?), ssl, ssl).
index(ssl_negotiate(?,?,?,?,?), ssl, ssl).
index(ssl_peer_certificate(?,?), ssl, ssl).
index(ssl_peer_certificate_chain(?,?), ssl, ssl).
index(ssl_session(?,?), ssl, ssl).
index(ssl_secure_ciphers(?), ssl, ssl).
index(verify_certificate(?,?,?), ssl, ssl).
index(verify_certificate_issuer(?,?), ssl, ssl).
index(ssl_upgrade_legacy_options(?,?), ssl, ssl).
index(xmld_signed_DOM(?,?,?), xmldsig, xmldsig).
index(xmld_verify_signature(?,?,?,?), xmldsig, xmldsig).
index(decrypt_xml(+,-,3,+), xmlenc, xmlenc).
index(load_certificate_from_base64_string(?,?), xmlenc, xmlenc).
